(() => {
	"use strict";

	window.addEventListener("load", () => {
		const $root = document.getElementById("cb__root");
		if (!$root) return;

		// placeholder (depois vamos substituir por UI real)
		// Mantendo simples pra não depender de libs agora.
		console.log("[charbuilder] loaded");
	});
})();
